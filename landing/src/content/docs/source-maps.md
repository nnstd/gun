---
title: Source Maps
lead: Pass --source-maps and Gun emits a .go.map alongside each Go file so production traces map back to the source.
sections:
  - Emit source maps
---

## Emit source maps

```bash
gun transpile src/ -o go/ --source-maps
```
